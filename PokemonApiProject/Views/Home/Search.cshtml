@model Root





@*<table class="pokemon-table">
    @for (int i = 0; i < Model.results.Count; i += 10)
    {
        <tr>
            @for (int j = i; j < i + 10 && j < Model.results.Count; j++)
            {
                <td>
                    <a href="https://pokemondb.net/pokedex/game/firered-leafgreen">
                        <div class="pokemon">
                            <img src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/@(j+1).png" alt="@Model.results[j].name">
                            <h1>@Model.results[j].name</h1>
                        </div>
                    </a>
                </td>
            }
        </tr>
    }
</table>*@

<table class="pokemon-table">
    @for (int i = 0; i < Model.results.Count; i += 10)
    {
        <tr>
            @for (int j = i; j < i + 10 && j < Model.results.Count; j++)
            {
                <td>
                    <a target ="_blank" href="https://pokemondb.net/pokedex/@(Model.results[j].name)">
                        <div class="pokemon">
                            <img src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/@(j+1).png" alt="@Model.results[j].name">
                            <h1>@Model.results[j].name</h1>
                        </div>
                    </a>
                </td>
            }
        </tr>
    }
</table>

